import { CodeVerseValidators } from './code-verse-validators';

describe('CodeVerseValidators', () => {
  it('should create an instance', () => {
    expect(new CodeVerseValidators()).toBeTruthy();
  });
});
